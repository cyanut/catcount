Scripts to help routine catFISH counting.

shuffle.py: softlink image batches into numbers in random order, so you can count them blindly.

dumpcnt.py: Collects batches of cell counting results (xml from ImageJ), analyze, output a nice bar-graph (by matplotlib) and some raw data. 
